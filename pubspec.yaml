name: impressao_etiqueta_logistica_shopee
description: Aplicativo para imprimir PDF em duas impressoras diferentes.
publish_to: "none" # Remove isso se for publicar no pub.dev um dia
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Nossas dependências
  printing: ^5.11.0
  pdf: ^3.10.0
  file_picker: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  msix: ^3.16.12

flutter:
  uses-material-design: true
